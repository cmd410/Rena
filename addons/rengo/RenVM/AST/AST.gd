extends Node
class_name RenAST

func visit(inter):
    """Visit function is supposed to actualy do some stuff
    and probably modify state of interpreter and call its
    method so send messages to the outside world.
    """
    assert(false, 'Visit function not implemented for node %s' % [self])
